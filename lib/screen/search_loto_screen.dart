import 'package:flutter/material.dart';

class SearchLotoScreen extends StatelessWidget {
  const SearchLotoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}